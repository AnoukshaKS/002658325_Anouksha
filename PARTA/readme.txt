Name: Anouksha Kolige Sreemurthy
NUID: 002658325
Mail: sreemurthy.a@northeastern.edu

<html> -> used to convert from web browser to HTML doc.
<link> -> used to mention the favicon/css styling files.
<title> -> used to show page name.
<input> -> used to invoke the data
<a> -> used to create a hyperlink for URL's
<div> ->  used for making sections within the page.
<head> -> tag contains info.
<ul> -> represents an unordered list
<style> -> used for styling an element/page.
<h1> -> its a header tag
<button> ->used for creating clickable button on the webpage.
<table> -> used to build a table which contains rows & columns.
<li> -> shows list of elements.
<tr> -> used for defining a rows
<p> -> represents the paragraph or content.
<th> -> used to define a header cell
<td> -> used to define data cells
<span> -> used to wrap sections of text
<br> -> used for single line break.